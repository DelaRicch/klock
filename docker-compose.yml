version: '3.8'
services:
  server:
    build:
    dockerfile: Dockerfile
    context: './server'
    volumes:
      - .:/app
    ports:
      - '8080:8080'
    command: ["air", "-c", ".air.toml"]
    tty: true