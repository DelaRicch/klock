import { MenuAnimationDirective } from './menu-animation.directive';

describe('MenuAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
